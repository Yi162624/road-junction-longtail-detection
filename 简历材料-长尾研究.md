# 简历材料 · 长尾/混淆路口检测受控实验研究

> 用途：简历/面试可直接使用的叙事草稿（中文）。
> 原则：所有数字均实测可查（见执行手册实验记录总表），不虚标；环形类只报告不作"显著提升"宣称。
> 记录日期：2026-09-04

---

## 一、一句话定位

遥感路口检测（十字/T型/环形 3 类）上的**长尾问题受控实验研究**：诊断出 T 型路口弱是"样本少 + 形态混淆"叠加，用 **close_mosaic 扫描定基线 + 冻结特征与类别加权的 2×2 消融**给出可归因的提升，全程受控、可复现。

---

## 二、可量化成果（诚实版）

| 指标 | base | base* | a3-decoupled（最优） |
|---|---|---|---|
| mAP50-95 | 0.317 | 0.3653 | **0.3763** |
| mAP50 | 0.530 | 0.6056 | 0.6164 |
| 十字 mAP50-95 | 0.503 | 0.5235 | 0.5439 |
| T型 mAP50-95 | 0.226 | 0.2723 | 0.2868 |
| 环形 mAP50-95 | 0.223 | 0.3001 | 0.2982（27框噪声内） |

- 全链路提升：mAP50-95 **0.317 → 0.3763（+0.059）**
- 关键方法层净增益（扣除"多训10轮"底座后）：**+0.006**
- 2×2 消融：a3-ref 0.3699 / a2加权 0.3678 / a3-ctrl冻结 0.3731 / **a3-decoupled 0.3763**

### 消融结论（一句话）

> 纯加权拉高 T 型但压崩环形；纯冻结稳但拉不动 T 型；**冻结+加权互补，三类全涨不砸任何一类**。

---

## 三、可讲的三个故事（STAR）

### 故事 A：数据诊断能力 —— 揭穿历史成绩的水分

- **S**：接手项目时验证集 mAP50-95 记录 0.468，看似可用
- **T**：我怀疑它归因不清，无法支撑简历
- **A**：
  1. 核查数据：旧训练集混入了环形增强副本（_roundabout_x1~x5）和 val 图片，属污染
  2. 实证 copy_paste 增强从未生效（数据无掩码，ultralytics 直接跳过，消融三组指标逐位相同）
  3. 重建纯净数据集 dataset_clean，重测真实水平 ≈0.32~0.33
- **R**：确立"同起点/同数据/同轮数"受控原则，淘汰虚高基线，为后续方法归因扫清障碍

### 故事 B：实验设计能力 —— close_mosaic 扫描定满血基线

- **S**：需要一条干净、可复现的基线作为所有方法对比的"尺子"
- **T**：基线本身不能带未受控变量
- **A**：固定 yolo11x.pt + 纯净数据 + 同一配置，只扫 close_mosaic∈{10,15,20}，patience=0 跑满 30 轮保公平，逐类指标选优
- **R**：base*=scan_cm15（0.3653，三类全涨无副作用）；并发现 close_mosaic=20 环形退化，证明"精修期过长反伤少类"

### 故事 C：方法改进能力 —— 解耦训练 + 类别加权 2×2 消融

- **S**：T 型 mAP50-95 只有十字的 ~52%（0.272 vs 0.524），疑似分类混淆
- **T**：验证两个干预各自/组合的效果，且必须可归因
- **A**：
  1. 冻结 = 结构层干预：原生 freeze 冻结 model.0~22 特征提取，只训检测头（防 backbone 被多数类带偏）
  2. 加权 = 损失层干预：BCE 分类损失挂 pos_weight（十字1.0/T型1.94/环形11.79，等价 class-balanced loss）
  3. 2×2 消融四组，同起点、同轮数（总训练量一致），单进程串行跑完
  4. 附 smoke test：先 1 轮验证冻结/加权真生效，避免白烧 GPU
- **R**：完整方法 0.3763 全场最高且十字/环形不降；归因清晰——纯加权伤环形（-0.038），冻结保护环形，组合互补

---

## 四、简历 bullet 候选（中文）

### 1 行版
> 在遥感路口检测竞赛中定位长尾"分类混淆"问题，通过 close_mosaic 扫描与解耦训练（冻结特征+类别加权 2×2 消融）将 mAP50-95 从 0.317 提升至 0.3763，全程受控可归因。

### 2 行版
> · 遥感路口检测（3类）长尾研究：揭穿历史 0.468 成绩的数据污染，重建纯净基线 base*（0.3653）
> · 设计 2×2 消融验证"冻结特征提取 + 分类损失类别加权"互补，最优解耦模型 mAP50-95 0.3763、三类不降

### 3 行版（STAR 展开，项目描述用）
> · 独立完成遥感路口检测长尾问题研究：诊断 T 型弱为"样本少+形态混淆"，用实验证明而非猜测
> · 统一 yolo11x.pt 起点/纯净数据/训练量，扫 close_mosaic 定基线并做 2×2 消融（冻结×加权），逐类归因
> · 最优解耦训练 mAP50-95 0.317→0.3763，十字 0.5439 全场最高；环形类如实标注噪声不虚标

---

## 五、面试追问准备

1. **为什么 T 型弱？样本少还是混淆？**
   → 两者兼有。A0 统计 T 型框 5,504（数量并不少）但 AP 只有十字一半 → 主因混淆；a2 加权只 +0.008、a3-ctrl 冻结 T 型基本不动，说明单手段都不够。

2. **pos_weight 为什么不用 Focal Loss？**
   → Focal 治"难易不平衡"（前背景），pos_weight 治"类间频率不平衡"（长尾），我们病根是类间，且 pos_weight 是 BCEWithLogitsLoss 原生参数、零代码改动、可控。

3. **环形权重 11.79 是不是拍脑袋？**
   → `w=(N_max/N_c)^0.5`，指数 0.5 是常用折中；消融显示纯加权下环形被压崩（11.79 过重），是实验暴露的，不是事后编的。

4. **冻结为什么用 freeze 参数而不是 requires_grad=False？**
   → ultralytics 训练器内部 setup_model() 会重建模型并强制解冻非白名单参数，手动 requires_grad 会被冲掉（实测 smoke 失败）。原生 freeze 在重建后应用，可靠。

5. **"净增益 +0.006"太少，方法是不是没用？**
   → base* 已是扫描后的较优基线，绝对空间小；且环形 val 仅 27 框评估噪声大。本研究的价值在**归因闭环**（每一步涨跌可指认来源），不是绝对分数。

6. **为什么还加了 a3-ref（纯续训）这一组？**
   → 没有它，"多训 10 轮"的收益会被误算成方法功劳。它是底座，保证净增益计算干净。

7. **冻结具体冻了什么？**
   → model.0~22（backbone+neck，特征提取），只留 model.23 检测头可训；dfl 层 ultralytics 默认永远冻结。

8. **close_mosaic=20 为什么环形差？**
   → 三组是从头独立训练的公平对比：scan_cm20 整体 mAP50-95 0.3350 < scan_cm15 的 0.3653，环形从 0.3001（cm15）掉到 0.1904（cm20）。mosaic 关得越早、无 mosaic 纯精修期越长（cm20 后 20 轮），干净图上训练越久，少类过拟合越重。这个"精修期过长反伤少类"的规律是 cm20 vs cm15 对比暴露的，不是同模型训练中的"回落"。

9. **跑一次训练多少钱/多久？数据量？**
   → AutoDL RTX 3090；dataset_clean train 4,002 张；全量 30 轮约 3 小时 ~6 元；消融四组串行共 4~5 小时。

10. **为什么不 merge val 进 train？**
    → 数据污染红线。merge 会让 val 失去独立性，分数虚高不可复现（历史 V4 之后就是这么污染的）。

---

## 六、已备好的证据链（面试时可展示）

- 执行手册：A0 统计 / 定稿配置 / 实验记录总表（7 行完整数据）
- runs_longtail/：base / scan_cm15 / scan_cm20 / a3_ref / a2_weighted / a3_ctrl / a3_decoupled（混淆矩阵 + PR 曲线 + results.csv）
- scripts/：train_base.py / train_scan_cm.py / train_a3.py（smoke test + 原生 freeze + pos_weight 补丁）

---

## 七、待办/可选后续

- [ ] 效果对照图（val 挑 3-5 张 T 型，改进前后画框对比）→ 有需要再做
- [ ] 英文版简历 bullet（如有海外投递需求）
- [ ] 复跑 a3 系列收 6 轮看能否更高（当前 best epoch 1~5，10 轮偏多）——可选，非必需
