# 长尾路口检测研究 —— A1~A5 执行手册

> 用途：按本手册执行"极端类别不平衡路口检测"研究，产出可写进简历的受控实验证据。
> 记录日期：2026-09-02
> 配套总览：见《高含金量方向方案.md》方向 A 部分（本文为可执行细化版）

---

## 〇、总原则（先读，后面所有步骤都遵守）

1. **受控实验三要素**：所有对比模型必须"同起点（yolo11x.pt）、同数据（dataset_clean）、同轮数"，只允许改动要研究的那一个变量。
2. **V2 出局**：V2/V1 是链式续训产物（历史脏、归因不清），一律不进本研究链。研究链从 yolo11x.pt 从零开始。
3. **禁止 merge val → train**（数据污染红线，已有教训）。
4. **不做 copy_paste**（已查明：数据无掩码，该增强不生效，扫了白扫）。
5. 每个模型训练完，**在同一份 val（499 张）上记录逐类指标**，最后汇总成表。
6. **过拟合控制**：所有续训（A3-ctrl/A3 的 5~10 轮）都要开 patience 早停 + 每轮盯 val loss；若 val loss 开始回升而 train loss 还在降，就是过拟合信号，立即停。因为 A3-ctrl 与 A3 同轮数，即使两者都轻微过拟合，对比仍然公平（同吃这个风险）。
7. **少类噪声声明**：实验前先统计 val 里每类的真值框数并记录；若某类（尤其环形）框数不足 100，该类 AP 波动大，结论里只报告、不当作"显著提升"宣称（详见 A5）。

### 为什么必须这么做（一句话解释给面试官听）

> 提升要"可归因"才有价值。V2 分数高但说不清是谁的功劳；本研究让每个模型同起点起步，每一点提升都能指认来源——这才是简历要的证据链。

---

## A0 数据统计结论（2026-09-02 实测，先读）

### 统计结果（dataset_clean，已验证准确）

统计方式：读标注文件行首类别计数；交叉验证通过（val 图片 499 / 含标注图 479 / 总行 3151 全部合法，20 张 val 图为无目标空图，正常）。

| 数据集 | 十字路口(0) | T型路口(1) | 环形路口(2) | 总框 |
|--------|:---:|:---:|:---:|:---:|
| train（4002 图） | 20,676 | 5,504 | 149 | 26,329 |
| val（479 含框图） | 2,389 | 735 | 27 | 3,151 |

### 结论（决定后续怎么做）

1. **环形（train 149 / val 27 框）**：真实极端少类，但 val 仅 27 框 → AP 测不准（噪声实锤）。且环形框高度集中在少数几张图。
   - 决策：**不投入专门优化**。训练照常带它；结论只报告 ± 波动、不作"显著提升"证据；简历不提。
2. **T 型（train 5,504 / val 735 框）**：数量并不少但 AP 最低（0.285）→ **病根是"难认/易混淆"，不是样本少**。
   - 决策：**研究主线**。735 个 val 框可测、结论可信。方法上 A3 解耦（治混淆）优先级高于 A2 重加权（治样本少）。
3. **十字（train 20,676 / val 2,389 框）**：样本充足、AP 稳定，作对照组与整体贡献主体。

### 对研究设计的影响

| 原设想 | 修正后 |
|--------|--------|
| 主攻环形 + T 型两个少类 | 只主攻 T 型（可测）；环形顺带观察不投入 |
| A2 重加权优先试 | **A3 解耦优先**（T 型是混淆问题）；A2 作对照 |
| 所有类都期望显著提升 | 十字/T 型出结论；环形明确降级 |
| val 噪声对策多档待选 | 直接定档：免费档（少类只报告不宣称），不必多 seed |

---

## A1 固定基线（base）—— 先立一个干净的尺子

### 1.1 为什么要做

- 研究要想对比，必须先有一个"干净对照组"（尺子）。
- V2 不能当尺子：它是 V1→V2→V3 续训来的，起点、轮数、增强都中途改过，你拿它对比，涨分说不清是方法的功劳还是起点不同。
- base = yolo11x.pt 从零训练，以后 A2/A3 全从它派生/跟它比，公平。
- **base 的配置 = 用干净受控方式复现 V2 的高分配置**（30 轮 + 后期无 mosaic 精修），目标让 base 尽量接近 V2 的 0.468，但机制全程干净、可归因、可复现（这点与历史 V2 本质不同）。

### 1.2 原理

检测器 = 特征提取（找路口）+ 分类头（认类别）。长尾问题主要出在分类环节——少类样本少，分类头决策边界被多数类带偏。base 先把这个"被带偏"的原始状态固定下来，后续改进才有参照物。

### 1.3 具体做法（云端 1 次训练）

| 配置项 | 值 |
|--------|----|
| 模型/起点 | yolo11x.pt（从零） |
| 数据 | /root/autodl-tmp/dataset_clean/dataset_clean.yaml |
| 轮数 | 30 |
| batch / imgsz | 8 / 1024 |
| optimizer / lr0 / lrf | AdamW / 0.001 / 0.01 |
| warmup / cos_lr | 5 / True |
| 增强 | mosaic 前段开 + 后期关（`close_mosaic=10`：前 20 轮 mosaic、后 10 轮无 mosaic 精修，近似 V2 结构；若想完全复现 V2 的"mosaic 只开前 5 轮"则设 `close_mosaic=25`，二选一后 A2/A3 全沿用同一值）、degrees 90、flip 0.5 |
| seed | 0（可复现） |
| 输出目录 | /root/autodl-tmp/runs_longtail/base |
| val | 每轮自动验证，plots=True（自动出混淆矩阵/PR/逐类 mAP） |

### 1.4 产出

- `best.pt`（base 权重，**同时是 A3 阶段一模型**）
- 混淆矩阵 + 逐类 mAP50/mAP50-95 ← 正式诊断图，替代 V2 诊断

### 1.4.1 预期与验收

- 参考：历史 V2 = 0.723 / 0.468（非受控：续训 + 总 40 轮当量，不可直接比）
- base 若达 **0.44~0.47（mAP50-95）** 视为成功复现 V2 水平
- 即使略低也不影响结论：A2/A3 的对比只发生在 base 这条干净链内部，不与历史 V2 挂钩
- 若 base 明显低于 0.44（如 <0.42）→ 先查配置是否写错（smoke test 先行），不急着进 A2/A3

> ⚠️ 上述验收线已在 2026-09-02 实测后作废，见下方 1.4.2 定稿决策第 2 条。

### 1.4.2 实际执行记录与定稿（2026-09-02，重要，后续全遵守）

**A1 base 已完成，直接作为正式基线，不重跑。**

- 运行环境：AutoDL RTX 3090 / ultralytics 8.4.137 / seed=0 / deterministic
- 实际轮数：设 30，实际 19 轮触发早停（patience=5），最佳 epoch = 14
- 实测结果（同一份 dataset_clean/val）：
  | base | mAP50 | mAP50-95 | 十字 | T型 | 环形 |
  |------|:---:|:---:|:---:|:---:|:---:|
  | 整体 | 0.530 | 0.317 | — | — | — |
  | 逐类 | — | — | 0.503（2389框） | 0.226（735框） | 0.223（27框，噪声仅报告） |
- best.pt 位置（云端）：/root/autodl-tmp/runs_longtail/base/weights/best.pt

**定稿决策（已于 2026-09-02 经用户逐条确认，正式生效）：**
1. **不重跑 base**：受控对比只需"同起点、同数据、同配置"，base 是否练满轮数不影响对比公平性（早停是标准流程，A2/A3 同配置也会同样早停）。
2. **作废 0.44~0.47 验收线**：历史 V2 分数部分来自旧 dataset_yolo 的环形增强副本（_roundabout_x1~x5），属虚高；dataset_clean 为纯净数据，其真实水平约 mAP50-95 0.32~0.33（同数据的 runs_scan 满 30 轮也仅 0.328 佐证）。后续不再拿 V2 分数当目标。
3. **统一标准配置（定死，A2/A3 只改研究变量，其余一字不改）**：
   - 起点 yolo11x.pt；数据 dataset_clean
   - epochs=30、patience=5、batch=8、imgsz=1024、workers=16
   - optimizer=AdamW、lr0=0.001、lrf=0.01、warmup_epochs=5、cos_lr=True、weight_decay=5e-4
   - seed=0、deterministic=True
   - close_mosaic=10（ultralytics 默认，不研究不调整）、degrees=90、fliplr=0.5、flipud=0.5、hsv_h=0.015、hsv_s=0.3、hsv_v=0.2
   - val=True、save=True、plots=True
   - 不做 copy_paste、不 merge val
4. base 权重同时充当 **A3 阶段一模型**（A3 直接加载 base/best.pt 做冻结重训）。
5. **简历叙事定位（前提认知）**：接受 base 低分（mAP50-95 0.317），整条研究链绝对分数不高（A3 有效也约 0.35）；简历卖点 = 受控实验严谨性 + 方法归因，**不追求对标 V2 0.468**。
6. **已知局限（不影响方法验证）**：base 的 backbone 止步于 mosaic 期（最佳 epoch 14 < 无 mosaic 精修期 21~30），特征非"最精"；A3 冻结该 backbone 只重训分类头，绝对提升可能略打折，但 base 与 A3 同特征对比，相对提升仍可信。

### 1.5 记录（训练完立即抄进表）

| base | 十字 | T型 | 环形 | mAP50 | mAP50-95 |
|------|:---:|:---:|:---:|:---:|:---:|
| 数值 | 0.503 | 0.226 | 0.223 | 0.530 | 0.317 |

> 记录位置：本文末尾《实验记录总表》，或写入 路口检测项目/模型测试记录.md
> 实测值已填（2026-09-02，详见 1.4.2）。

---

## A2 类平衡重加权 —— 给少类"调音量"

### 2.1 为什么要做

- 三类样本量差距大（以 A0 统计实测为准，环形/T 型可能远少于十字）。
- 模型训练时每个框贡献一份学习压力，少类框少 → 少类梯度占比小 → 模型"懒得学"少类，AP 自然低。

### 2.2 原理（讲给面试官）

训练损失是所有框损失之和。给少类的框乘一个更大的权重（如 T 型 ×3），相当于"错判一个 T 型 = 错判 3 个十字"，把模型的学习注意力扳向少类。这是长尾学习两大流派之一"损失重加权"，论文依据充分（如 Focal Loss 思想的类别版）。

### 2.3 具体做法（云端 1 次训练）

- 配置与 A1 base **完全相同**（同起点、同 30 轮、同增强），唯一改动：
- **修改 ultralytics 损失代码**：在 cls/box 损失里按该框类别乘权重
  - 权重公式（先简单后调）：`w_c = (N_max / N_c)^alpha`，alpha 初始 0.5（N_c 为 c 类框数，N_max 为最多类框数）
- 输出目录：/root/autodl-tmp/runs_longtail/a2_weighted

### 2.4 预期与判断

- 若 A2 的 T 型/环形 AP 相对 base 提升 → 病根确认是"样本少"，重加权有效
- 若几乎不变 → 病根不是数量，而是"形态混淆"（转到 A3，它是解药）

### 2.5 记录

| A2 | 十字 | T型 | 环形 | mAP50 | mAP50-95 | vs base |
|----|:---:|:---:|:---:|:---:|:---:|:---:|
| 数值 | 0.5484 | 0.2931 | 0.2619 | 0.6206 | 0.3678 | 见末尾总表 |

> 2026-09-04 定稿版：A2 改为 base* 续训 10 轮 + pos_weight（与 A3 同框架），实测值见末尾《实验记录总表》。

---

## A3 解耦重训（★核心方法）—— 只治"认错"的毛病

### 3.1 为什么要做

- A2 是"全局放大少类"，副作用：少类权重太高可能把定位（找框）也带乱。
- 长尾的真正病灶在**分类环节**：特征提取得再好，分类头决策边界被多数类带偏，少类照样被误判。
- A3 把"定位"和"分类"解耦，只针对分类环节手术，其他能力（backbone）完全不动。

### 3.2 原理（长尾检测经典做法，LVIS 相关工作，含金量所在）

两阶段：
- 阶段一：全量数据正常训练 → 特征提取成熟（**直接复用 A1 的 base，不另训**）
- 阶段二：
  1. 冻结 backbone（参数锁死，保护已有能力）
  2. 只保留并训练分类头
  3. 喂**类别均衡**样本流（三类每类一样多，来自均衡采样器或重复采样少类）
  4. 训练 5~10 轮 → 分类头在均衡样本下把少类决策边界掰正

比喻：学生各科都会，就"总把 T 型题当十字题做"。不重学全部科目（怕学乱），只针对这一个毛病，拿均衡题库单独刷分类这一关。

### 3.3 设计定稿（2026-09-04，2×2 析因消融，本研究的执行标准）

> 定稿原因：旧版"阶段二喂均衡采样器"= 数据层均衡，社区主流是 RFS 采样器而非复制文件，且 ultralytics 插自定义采样器易出 bug；等价且改面最小的是**损失层 pos_weight**（社区 class-balanced loss 标准实现）。同时为保证"总训练量一致"，全部统一为续训 10 轮 + patience=0，轮数不再是变量。原 A2/A3 旧做法作废，以本定稿为准。

**实验矩阵**（起点全为 scan_cm15/best.pt = base*，续训 10 轮、patience=0 跑满）：

| 实验 | 冻结 backbone | pos_weight 加权 | 输出目录 |
|------|:---:|:---:|------|
| A3-ref | ❌ | ❌ | runs_longtail/a3_ref |
| A2 | ❌ | ✅ | runs_longtail/a2_weighted |
| A3-ctrl | ✅ | ❌ | runs_longtail/a3_ctrl |
| A3 | ✅ | ✅ | runs_longtail/a3_decoupled |

**两个研究变量（互不影响，分开消融）**：
- 冻结 = 结构层：用 ultralytics 原生 `freeze` 参数冻结 model.0~22（backbone+neck 特征提取），只留检测头 model.23 可训练。不用手动 requires_grad——训练器内部 setup_model() 会重建模型，外部冻结会被冲掉（实测），原生 freeze 白名单机制重建后依然生效
- pos_weight = 损失层：monkeypatch `v8DetectionLoss.__init__`，把 `self.bce` 换成带 pos_weight 的 `BCEWithLogitsLoss`（PyTorch 原生参数，不碰 site-packages 源码、不复制数据）

**类别权重**（`w=(N_max/N_c)^0.5`）：十字=1.0、T型≈1.94、环形≈11.79

**统一续训配置（四组一字不改）**：lr0=0.0001、lrf=0.01、epochs=10、patience=0、batch=8、imgsz=1024、AdamW、warmup_epochs=0、cos_lr=True、weight_decay=5e-4、seed=0、deterministic=True、mosaic=0、degrees=0、fliplr=0.5、flipud=0.5、val=True、plots=True

**归因逻辑**：
- A3-ref vs base*：纯多训 10 轮的收益（报告时须扣除，不能算方法功劳）
- A2 vs A3-ref：加权的净贡献（全参数下）
- A3-ctrl vs A3-ref：冻结的净贡献
- A3 vs A3-ctrl：冻结基础上，加权的额外贡献
- A3 vs A2：全参数基础上，冻结的额外贡献

### 3.3.1 正式训练前必做：smoke test（冒烟测试）

目的：先花几分钟验证"冻结 + 加权代码改对了"，避免白烧 GPU 几小时才发现实现错了。

做法：先训 1 轮，检查三点：
- [ ] 检测头（model.23）参数确实在更新（训练能跑、loss 在降）
- [ ] 冻结生效：a3.log 里出现大量 `Freezing layer 'model.X...'`（X=0~22 均出现）
- [ ] pos_weight 权重张量打印无误（十字/T型/环形 权重与设定一致）
通过后再上 3.3 的四组正式训练。

### 3.4 判断标准

- 对比 base：若 T 型 AP 提升且十字 AP 不掉（或微掉）→ 解耦有效，这就是简历的核心结果。
- 若 A2 无效果而 A3 有效 → 完美印证"病根在分类头"，叙事闭环成立。

### 3.5 记录

| A3 | 十字 | T型 | 环形 | mAP50 | mAP50-95 | vs base |
|----|:---:|:---:|:---:|:---:|:---:|:---:|
| a3-ctrl（只冻结） | 0.5428 | 0.2837 | 0.2929 | 0.6205 | 0.3731 | 见末尾总表 |
| a3-decoupled（冻结+加权） | 0.5439 | 0.2868 | 0.2982 | 0.6164 | 0.3763 | 见末尾总表 |

> 2026-09-04 定稿版：A3 系列 = base* 续训 10 轮 2×2 消融四组（a3-ref/a2/a3-ctrl/a3-decoupled），实测值见末尾《实验记录总表》。

---

## A4（可选）损失级进阶 —— 预算允许再上

### 4.1 目的

再验证一个"损失层面"的改进（如 Seesaw Loss 思路 / 类别分档 box 增益），和 A2/A3 一起进对比表，实验更饱满。

### 4.2 做法（云端 +1 次）

- 配置同 base，loss 换成 Seesaw Loss 思路（改分类损失实现）
- 输出 runs_longtail/a4_seesaw

### 4.3 记录

| A4 | 十字 | T型 | 环形 | mAP50 | mAP50-95 | vs base |
|----|:---:|:---:|:---:|:---:|:---:|:---:|
| 数值 | ? | ? | ? | ? | ? | Δ? |

---

## A5 汇总与简历产出

> 状态：2026-09-04 A3 系列 2×2 消融已跑完，下表数据来自同一份 val（dataset_clean/val，499 张），全部实测。

### 5.1 消融总表（核心交付物）

| 方法 | 冻结 | 加权 | 十字 | T型 | 环形 | mAP50 | mAP50-95 | 净增益(vs base*) |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| base（cm=10，对照） | — | — | 0.503 | 0.226 | 0.223 | 0.530 | 0.317 | — |
| base*（scan_cm15） | — | — | 0.5235 | 0.2723 | 0.3001 | 0.6056 | 0.3653 | — |
| a3-ref（续训底座） | ❌ | ❌ | 0.5392 | 0.2851 | 0.2853 | 0.6203 | 0.3699 | +0.005（须扣除） |
| a2（只加权） | ❌ | ✅ | 0.5484 | **0.2931** | 0.2619 | 0.6206 | 0.3678 | -0.002（底座后） |
| a3-ctrl（只冻结） | ✅ | ❌ | 0.5428 | 0.2837 | 0.2929 | 0.6205 | 0.3731 | +0.003（底座后） |
| **a3-decoupled** | ✅ | ✅ | **0.5439** | 0.2868 | **0.2982** | 0.6164 | **0.3763** | **+0.006（底座后）** |

> 注：base* 是 A2/A3 系列的起点（30 轮），A3 系列在其上续训 10 轮，总轮数统一（40 轮）。
> 净增益 = 相对 base* 的增益 减去 a3-ref 的增益（即扣除"多训 10 轮"本身的收益）。

### 5.2 结论撰写（已按实测填写）

**1. T 型弱是"样本少"还是"形态混淆"？**

两者兼有，且两种干预方向都单独有效但不彻底：
- a2（加权 = 治样本少）：T 型涨到全场最高 0.2931，但环形被过重权重（11.79）压崩到 0.2619——纯加权有副作用
- a3-ctrl（冻结 = 治混淆）：T 型只涨到 0.2837，但环形保住 0.2929——冻结稳但拉动弱
- 结论：T 型病根是"样本少 + 易混淆"叠加，单一手段都不能同时解决；冻结与加权互补

**2. 最优方法把整体/T 型提升多少？**

- 最优 = **a3-decoupled（冻结+加权）**：mAP50-95 0.3653 → 0.3763（毛 +0.011；扣底座净 +0.006）
- T 型 0.2723 → 0.2868（毛 +0.015）；十字 0.5235 → 0.5439（+0.020，全场最高）

**3. 有没有副作用？**

- 十字：0.5439 全场最高，无副作用
- 环形：0.2982 vs base* 0.3001，跌 0.002——在 val 仅 27 框的噪声范围内，**不作为副作用证据，只报告不宣称**
- 对比 a2（纯加权）：环形跌到 0.2619（-0.038），副作用明显 → 说明冻结保护了环形

**方法叙事（给面试官）**：

> 长尾路口检测中 T 型 mAP50-95 只有十字的 ~52%。我用 2×2 消融验证两个干预：冻结特征提取器（防 backbone 被多数类带偏）与分类损失类别加权（放大少类梯度）。单独用都有效但有短板——纯加权伤环形、纯冻结拉不动 T 型；组合（a3-decoupled）取得全场最高 0.3763 且十字/环形不降，证明"结构解耦 + 损失再平衡"互补。绝对分数温和（base* 已较优 + 环形 val 27 框评估噪声大），**研究卖点是受控实验的可归因性**。

### 5.2.1 技术记录（诚实备注）

1. **best epoch 偏早**：a3_ref/a3-ctrl/a3-decoupled 的 best epoch 在 1~5 轮，第 10 轮反而略降 → 10 轮续训偏多，若复跑应收 6 轮或加 patience。四组同轮数，对比仍公平，不影响结论。
2. **环形降级**：val 仅 27 框，环形相关数字（0.26~0.30）波动大，一律只报告不作"显著提升"宣称。
3. **冻结机制**：必须用 ultralytics 原生 `freeze` 参数（训练器内部 setup_model() 重建模型，手动 requires_grad 会被冲掉），冻结 model.0~22 留检测头。

### 5.3 可视化素材清单

- [x] base 混淆矩阵（改进前的"病"）→ runs_longtail/base
- [x] 最优模型混淆矩阵（改进后的"好"）→ runs_longtail/a3_decoupled
- [x] 三类 PR 曲线（base* vs a3_decoupled，逐类对比）→ runs_longtail/scan_cm15 + a3_decoupled
- [ ] 典型 T 型检测效果对照图（改进前后，val 挑 3-5 张）→ 待做（需用 best.pt 跑推理画框）

---

## 六、云端训练次数与成本汇总

| 训练 | 内容 | 次数 | 单价 | 依赖 |
|------|------|:---:|:---:|------|
| A1 base | 从零 30 轮 | 1 | ~6 元 | 无 |
| scan_cm15/20 | close_mosaic 扫描（已跑完） | 2 | ~6 元/组 | A1 |
| A2 加权 | base* 续训 10 轮 + pos_weight | 1 | ~2 元 | scan_cm15（需 base* 权重） |
| A3-ctrl | base* 续训 10 轮 + 冻结 | 1 | ~2 元 | scan_cm15 |
| A3 | base* 续训 10 轮 + 冻结 + pos_weight | 1 | ~2 元 | scan_cm15 |
| A3-ref | base* 续训 10 轮（对照底座） | 1 | ~2 元 | scan_cm15 |
| A4（可选） | 从零 30 轮 + Seesaw | 1 | ~6 元 | A1 |

总计：A3 系列 4 组（必做）≈ 8-12 元。

### 云端执行顺序

```
scan_cm15(base*) ──┬─→ A3-ref（纯续训对照）
                   ├─→ A2（续训 + 加权）
                   ├─→ A3-ctrl（续训 + 冻结）
                   └─→ A3（续训 + 冻结 + 加权）
全部跑完 ──→ 下载结果 ──→ A5 汇总
```

---

## 七、实验记录总表（每训完一次填一行）

> **先记录 val 各类真值框数**（决定少类 AP 是否可信）：跑一次统计脚本得到「十字 _ 框 / T型 _ 框 / 环形 _ 框」，写入下表备注。环形 <100 框时，其 AP 只报告不作"显著提升"结论。

| 日期 | 实验 | 起点 | 轮数 | 十字 | T型 | 环形 | mAP50 | mAP50-95 | 备注 |
|------|------|------|:---:|:---:|:---:|:---:|:---:|:---:|------|
| 2026-09-02 | base（close_mosaic=10） | yolo11x.pt | 19（早停，best=14） | 0.503 | 0.226 | 0.223 | 0.530 | 0.317 | 对照组，patience=5 早停 |
| 2026-09-03 | scan_cm15（close_mosaic=15） | yolo11x.pt | 30（best=24） | 0.5235 | 0.2723 | 0.3001 | 0.6056 | 0.3653 | **base\* 选定**，三类全涨无副作用，整体最优 |
| 2026-09-03 | scan_cm20（close_mosaic=20） | yolo11x.pt | 30（best=29） | 0.5253 | 0.2892 | 0.1904 | 0.5727 | 0.3350 | T型最高但环形退化，整体低于 cm15 |
| 2026-09-04 | a3-ref（纯续训对照） | scan_cm15/best.pt | 10 | 0.5392 | 0.2851 | 0.2853 | 0.6203 | 0.3699 | 底座：多训10轮本身 +0.005（须扣除） |
| 2026-09-04 | a2_weighted（续训+加权） | scan_cm15/best.pt | 10 | 0.5484 | **0.2931** | 0.2619 | 0.6206 | 0.3678 | vs a3-ref：T型+0.008，但环形掉（权重11.79过重） |
| 2026-09-04 | a3-ctrl（续训+冻结） | scan_cm15/best.pt | 10 | 0.5428 | 0.2837 | 0.2929 | 0.6205 | 0.3731 | vs a3-ref：冻结净贡献 +0.003，保护环形 |
| 2026-09-04 | a3_decoupled（冻结+加权） | scan_cm15/best.pt | 10 | **0.5439** | 0.2868 | **0.2982** | 0.6164 | **0.3763** | 完整方法：全场最高，十字/环形最优无副作用 |

> 所有数值均来自同一份 val（dataset_clean/val，499 张），保证可比。
